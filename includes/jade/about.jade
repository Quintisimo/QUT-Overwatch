section#about
  h1 Who are we ?
  
  #staff
    each person, index in staff
      .person 
        .person-face
          img.avatar(src='#{ person[0] }', title="Click to see battletag")
        .player-avatar
          img.avatar(src='#{ person[1] }')
          .player-id(title="Click to see global ranking")
            span #{ index }
            
            each rank, index in ranks
              if index == person[2]
                img.rank(src='#{ rank }')
      
  p #{ about }
